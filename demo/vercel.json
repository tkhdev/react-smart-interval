{
  "buildCommand": "cd .. && npm install && npm run build && cd demo && npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "cd .. && npm install && cd demo && npm install"
}
